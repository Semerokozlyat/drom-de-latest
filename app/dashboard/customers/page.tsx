import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Invoices',  // this title will be set to the template placeholder inside /app/layout.tsx
}

export default function Page() {
    return <p>Customers Page</p>;
}